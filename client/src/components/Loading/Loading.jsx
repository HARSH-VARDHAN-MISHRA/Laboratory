import React from 'react';
import './Loading.css'; // Make sure to create and import a CSS file for styling

const Loading = () => {
  return (
    <div className="loading-spinner">
      <div className="spinner"></div>
    </div>
  );
}

export default Loading;
